fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.5

# PDF parsing / rendering (independent extraction methods, Layer 1 comparison)
pymupdf==1.25.2
pdfplumber==0.11.5
pdfminer.six==20231228

# DOCX parsing / rendering
python-docx==1.1.2
mammoth==1.8.0

# Synthetic test-fixture generation only (never used on real resumes)
reportlab==4.2.5

# LLM-powered scoring (career narrative, seniority, bullet quality, JD matching) --
# optional at runtime, degrades gracefully with no ANTHROPIC_API_KEY set.
anthropic==0.40.0
python-dotenv==1.0.1

# Tests
pytest==8.3.4
